@model TransactionForecastHitViewModel

@using (Html.BeginForm("SaveForecastHit", "Planning", FormMethod.Post, new { id = "createTransactionForecastHitForm", @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    @Html.EditorForModel()
    <div class="form-group">
        <div class="col-sm-offset-3 col-sm-9">
            <button class="btn btn-default">@Resource.Actions_Save</button>
        </div>
    </div>
}
<script type="text/javascript">
    $(function() {
        $('#createTransactionForecastHitForm').submit(function() {
            NBlackout.MoneyManager.Configuration.Planning.ForecastHitsPartial.ForecastHits.save({
                form: $(this),
                success: function() {
                    noty({
                        text: '@Resource.Messages_CreationDone'
                    });

                    $(document).trigger('forecast-hits-updated');
                }
            });

            return false;
        });
    });
</script>