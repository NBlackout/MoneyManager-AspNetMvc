@model UpdateEmailViewModel

@using (Html.BeginForm("SaveEmail", "User", FormMethod.Post, new { id = "editEmailForm", @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    @Html.EditorForModel()
    <div class="form-group">
        <div class="col-sm-offset-3 col-sm-9">
            <button class="btn btn-default">@Resource.Actions_Save</button>
        </div>
    </div>
}
<script type="text/javascript">
    $(function() {
        $('#editEmailForm').submit(function() {
            NBlackout.MoneyManager.Security.User.Email.save({
                form: $(this),
                success: function(response) {
                    noty({
                        text: '@Resource.Messages_ModificationDone'
                    });
                }
            });

            return false;
        });
    });
</script>